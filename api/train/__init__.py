"""NIRIKSHAK - the confirmation loop (P11).

Where trust originates. `api/learn/` proposes a mapping for a line no pack
recognises; nothing it produces is ever a fact. An administrator confirms or
corrects, and *that recorded decision* — a named person, an outcome, an audit
sequence — compiles into a versioned vendor pack. The next parse matches it
deterministically, and the line stops being unknown.

    queue.py       residue as one decision at a time, honest about the model
    compile.py     a confirmed line becomes a boring, readable pattern
    activation.py  DRAFT -> VALIDATED -> ACTIVE, and back again
    service.py     the loop, and the five audit records it writes

This package composes `learn`, `db`, `audit`, `ingest.packs` and `models`
(decision D44). It exists so that composition has a home other than
`api/learn/`, which stays unable to reach storage: an advisory branch that could
persist would eventually persist something nobody confirmed.

Nothing here reaches a verdict, a canonical field, a remediation command or a
report. Coverage grows one way only, and it runs through a person.
"""
